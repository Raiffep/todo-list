import { StyleSheet } from "react-native";
import { SQUARE_BUTTON, colors } from "../../globalStyles";

export const styles = StyleSheet.create({
  container: SQUARE_BUTTON.button,
  buttonIcon: {
    width: 16,
    height: 16,
  },
});